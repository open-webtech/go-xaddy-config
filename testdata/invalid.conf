# Invalid configuration for error testing
log_level

# Unclosed block
server broken {
    listen 80

# Missing closing brace

# Invalid directive with block
simple_directive value {
    should_not_have_block true
}